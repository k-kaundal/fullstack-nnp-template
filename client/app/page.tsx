import HomeClient from './HomeClient';

/**
 * Home page with enhanced 3D animations and scroll effects
 * Uses HomeClient for animated content with React Three Fiber
 */
export default function Home() {
  return <HomeClient />;
}
